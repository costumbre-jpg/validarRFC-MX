apiVersion: v1
kind: Service
metadata:
  name: {{ include "validarfc.fullname" . }}
  labels:
    {{- include "validarfc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "validarfc.selectorLabels" . | nindent 4 }}
